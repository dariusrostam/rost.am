PDFs referenced by `publications.yaml` (`pdf: /papers/<slug>.pdf`) go in this folder.
Filenames must match the `pdf` field exactly. See `publications.yaml`'s top-of-file
TODO note for which entries still need a file dropped in here.
